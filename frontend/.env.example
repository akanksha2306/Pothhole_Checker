# PotholeWatch frontend environment
# Copy to .env and fill in real values.

# Google Identity Services OAuth client id (issued in Google Cloud Console).
# Posted to the backend as an ID token; the backend exchanges it for a session cookie.
# Left empty, the login page shows a disabled sign-in state instead of the Google button.
VITE_GOOGLE_CLIENT_ID=
